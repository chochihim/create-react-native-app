{
  "private": true,
  "scripts": {
    "format": "prettier --single-quote --trailing-comma=es5 --print-width=100 --write '{create-react-native-app,react-native-scripts}/{src,template}/**/*.js'",
    "format-template": "prettier --tab-width 4 --single-quote --trailing-comma=es5 --print-width=100 --write 'react-native-scripts/template/app/**/*.js'"
  },
  "devDependencies": {
    "prettier": "^0.21.0"
  }
}
